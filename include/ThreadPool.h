#ifndef THREADPOOL_H
#define THREADPOOL_H

#include <thread>

using std::thread;

// Template function for addThread function parameter
template <typename T> T

class ThreadPool {
    public:
        void addThread() 
    private:
        std::vector<thread> threadPool;
}

#endif
